const add = (a, b) => a + b;

const substract = (a, b) => a - b;

const multiply = (a, b) => a * b;

const divide = (a, b) => {
    if (a === 0){
        return 0;
    }
    else if( b === 0) {
        return console.log('Error');
    }
    else {
        a / b
    }
};





